# ---- Part 1: Conversational / Agentic AI ----
openai>=1.50               # one client for every provider. Gemini included: Google
                           # publishes an OpenAI-compatible endpoint, so its own SDK
                           # is not needed and is deliberately not installed.
python-dotenv>=1.0         # loads .env, keeps secrets out of the repo
pydantic>=2.0              # the extraction schema / validation contract
rich>=13.0                 # readable CLI output

# ---- Part 2: Machine Learning ----
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
# NOTE: xgboost deliberately omitted. sklearn's HistGradientBoostingClassifier is the
# tree candidate used in model comparison and needs no OpenMP system library. Logistic
# regression wins on the supplied data, and permutation importance explains the winner.
matplotlib>=3.9
joblib>=1.4                # model persistence

# ---- Serving ----
fastapi>=0.115
uvicorn[standard]>=0.32

# ---- Notebook ----
jupyter>=1.1
ipykernel>=6.29

# ---- Tests ----
pytest>=8.3
